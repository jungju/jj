{
  "acceptance_criteria": [
    "No persisted artifact, served dashboard response, jj status output, or jj runs output contains raw API keys, common token patterns, private key blocks, configured secret values, raw command text, raw environment values, unsafe absolute paths, omission placeholders, raw artifact bodies, raw manifests, raw diff bodies, validation output payloads, planner handoff payloads, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, or attacker-controlled denied path payloads.",
    "Traversal attempts using relative, absolute, encoded, or symlinked paths cannot read or write outside the configured workspace or allowed run artifact roots.",
    "Dry-run and non-dry-run produce equivalent redaction, boundary, diagnostic, validation output, planner handoff metadata, git diff redaction behavior, artifact persistence, and safe path label persistence.",
    "Existing TASK.md summary, latest-run summary, next-action summary, Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run, Validation Status dashboard behavior, run detail Validation Evidence, Compare Previous, Run Artifacts behavior, jj status behavior, and jj runs behavior remain unchanged when sanitized presentation helper maintenance or UI cleanup is performed.",
    "jj status and jj runs exit successfully in a valid workspace, use only shared sanitized DTO/helper output, and render deterministic none, unavailable, unknown, or denied states for malformed, denied, stale, hostile-label, token-like, partial, tied timestamp, missing timestamp, or internally inconsistent metadata without panics or unsafe reflection.",
    "Focused CLI and serve tests cover the sanitized status, recent-runs, dashboard root, TASK summary, run detail, validation evidence, compare-previous, run artifact states, Project Docs, Evaluation Findings, Latest Run action-link, Active Run, and Validation Status dashboard behavior states touched by each task.",
    "Completed security guardrails remain closed after TASK-0100 because focused CLI tests, go test ./internal/cli, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0101 introduced only narrow unexported internal/serve helper logic for shared sanitized dashboard summary-row construction.",
    "TASK-0101 consolidated Latest Run, Recent Runs, and Evaluation Findings dashboard summary-line formatting through the shared unexported dashboard summary-row helper.",
    "TASK-0101 preserved dashboard root behavior for TASK summary, latest-run summary, next-action summary, Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run, and Validation Status, including labels, ordering, spacing, visibility, links, timestamps, fallback messages, and denied, unavailable, unknown, none, empty, malformed, stale, hostile-label, token-like, partial, and inconsistent metadata states for touched paths.",
    "TASK-0101 consumes only existing sanitized DTO/helper output, guarded URL decisions, fixed labels, safe state metadata, safe timestamps or counts, and already-safe fallback text.",
    "TASK-0101 introduced no route, CLI flag, schema field, config behavior, scanner, upload, raw export, broad file read, raw manifest read, raw artifact read, raw TASK.md or EVAL.md body read, visual redesign, redaction policy change, path-boundary policy change, validation behavior change, or user-facing semantic change.",
    "TASK-0101 added focused internal/serve normal and fallback coverage for the touched Latest Run, Recent Runs, and Evaluation Findings dashboard summary rows, plus structural guard coverage that the touched helpers continue to use the shared summary-row helper.",
    "TASK-0101 validation passed focused serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0101 because focused serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0103 introduced only narrow unexported internal/serve helper logic for sanitized dashboard Next Action presentation construction and wired dashboard root sections through the shared presentation helper.",
    "TASK-0103 preserved TASK summary and Next Action labels, ordering, spacing, visibility, links, timestamps or counts, fallback messages, and denied, unavailable, unknown, none, empty, malformed, stale, hostile-label, token-like, partial, and inconsistent metadata states for touched paths.",
    "TASK-0103 consumes only existing sanitized DTO/helper output, guarded URL decisions, fixed labels, safe state metadata, safe run ID labels, safe timestamps or counts, and already-safe fallback text.",
    "TASK-0103 introduced no route, CLI flag, schema field, config behavior, scanner, upload, raw export, broad file read, raw manifest read, raw artifact read, raw TASK.md or EVAL.md body read, visual redesign, redaction policy change, path-boundary policy change, validation behavior change, or user-facing semantic change.",
    "TASK-0103 added focused internal/serve normal and fallback coverage for touched TASK summary and Next Action dashboard states, plus structural guard coverage that the touched helpers continue to use shared guarded helper logic.",
    "TASK-0103 validation passed focused serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0103 because focused serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0104 introduced only narrow unexported internal/serve helper logic for sanitized run-detail header and timestamp, top action-link, Validation Evidence, Compare Previous, and Run Artifacts fallback presentation construction.",
    "TASK-0104 wired touched run-detail presentation paths through helper-built view fields while preserving exact labels, ordering, spacing, visibility, guarded links, timestamps or counts, fallback messages, and denied, unavailable, unknown, none, empty, malformed, stale, hostile-label, token-like, partial, and internally inconsistent metadata behavior.",
    "TASK-0104 consumes only existing sanitized DTO/helper output, guarded URL decisions, fixed labels, safe state metadata, safe timestamps or counts, and already-safe fallback text.",
    "TASK-0104 introduced no route, CLI flag, schema field, config behavior, scanner, upload, raw export, broad file read, raw manifest read, raw artifact read, raw diff read, raw validation payload read, raw TASK.md or EVAL.md body read, visual redesign, redaction policy change, path-boundary policy change, validation behavior change, or user-facing semantic change.",
    "TASK-0104 added focused internal/serve normal, fallback, and hostile-action coverage for touched run-detail presentation paths, plus structural guard coverage that touched run-detail inspection routes continue to use shared guarded helper logic.",
    "TASK-0104 validation passed focused internal/serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0104 because focused internal/serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0105 polished jj serve dashboard root visual hierarchy, spacing, typography, status and fallback styling, action-link affordance, and responsive scanability for TASK summary, Latest Run, Next Action, Project Docs, Recent Runs, Evaluation Findings, Active Run, and Validation Status.",
    "TASK-0105 preserved existing dashboard root labels, ordering, visibility, guarded links, timestamps or counts, fallback messages, and denied, unavailable, unknown, and none states for touched dashboard paths.",
    "TASK-0105 consumed only existing sanitized DTO/helper output, Go template escaping, and guarded URL decisions.",
    "TASK-0105 introduced no route, CLI flag, schema field, config behavior, raw export, broad file read, scanner, upload, redaction policy change, path-boundary policy change, validation behavior change, or raw artifact, manifest, diff, validation, TASK.md, EVAL.md, or document body exposure.",
    "TASK-0105 added focused internal/serve dashboard root presentation, ordering, and guarded-link coverage, plus centralized sanitized presentation helper guard coverage.",
    "TASK-0105 validation passed go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0105 because focused serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0106 polished jj serve run-detail visual hierarchy, spacing, typography, status and fallback styling, action-link affordance, and responsive scanability for the run-detail header, run ID/status/timestamp metadata, top action links, Validation Evidence, Compare Previous, and Run Artifacts.",
    "TASK-0106 preserved existing run-detail labels, ordering, visibility, guarded links, timestamps or counts, fallback messages, and denied, unavailable, unknown, none, empty, malformed, hostile-label, token-like, partial, and internally inconsistent metadata behavior for touched paths.",
    "TASK-0106 consumed only existing sanitized DTO/helper output, Go template escaping, fixed labels, safe state metadata, safe timestamps or counts, and guarded URL decisions.",
    "TASK-0106 introduced no route, CLI flag, schema field, config behavior, scanner, upload, raw export, broad file read, raw manifest, raw artifact, raw diff, raw validation payload, raw TASK.md, raw EVAL.md, or raw document body exposure, redaction policy change, path-boundary policy change, validation behavior change, or new user-facing data semantics.",
    "TASK-0106 added focused internal/serve tests covering run-detail section ordering, labels, guarded href behavior, fallback text, representative normal and denied, unavailable, unknown, and none states, and touched presentation class or structure expectations.",
    "TASK-0106 validation passed go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0106 because focused internal/serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current validation, test, disclosure, or boundary evidence shows a concrete regression."
  ],
  "created_at": "2026-04-28",
  "goals": [
    "Generate concrete specs and tasks from a single plan file while preserving an auditable run history.",
    "Keep generated artifacts, served files, plan inputs, workspace mutations, validation outputs, git diff artifacts, planner and Codex handoff metadata, dashboard summaries, active-run progress, validation status, run-detail validation evidence, compare-to-previous links, run artifact inventory, CLI status output, and CLI recent-run output inside the resolved workspace or guarded run artifact roots.",
    "Prevent secrets, token-like values, private key material, omission placeholders, raw commands, raw environment values, unsafe paths, raw manifests, raw artifact bodies, raw diff bodies, validation output payloads, planner handoff payloads, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, and attacker-controlled denied payloads from being persisted or rendered.",
    "Provide a dashboard-first local jj serve experience with guarded run detail, history, comparison, sanitized audit export views, compact sanitized TASK.md work-queue summary, latest-run summary, next-action summary, Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run progress, Validation Status, Validation Evidence, Compare Previous, and Run Artifacts.",
    "Improve the jj serve UI so current task state, run status, evaluation findings, validation status, document shortcuts, and run-detail evidence are easier to scan without changing data semantics.",
    "Provide compact sanitized CLI summaries for quick workspace pre-flight and run-history checks without starting jj serve.",
    "Preserve dry-run and non-dry-run workflows with identical redaction, path-boundary, diagnostics, artifact persistence, diff redaction, validation, and planner handoff metadata behavior.",
    "Ensure auto and balanced task proposal resolution selects bugfix or security only from concrete current evidence, not completed guardrails, healthy context, or absent evidence.",
    "Use scripts/validate.sh as the single documented release validation entry point for local and CI security release gates.",
    "Keep completed security guardrails closed unless a concrete release-gate, CI, validation, test, disclosure, or boundary regression is observed.",
    "Keep sanitized CLI, dashboard root, run-detail, TASK summary, next-action, Project Docs, Recent Runs, Evaluation Findings, Active Run, Validation Status, Latest Run action-link, Validation Evidence, Compare Previous, Run Artifacts, and run-summary presentation code maintainable by reducing unnecessary duplication, nesting, and branching without changing output semantics."
  ],
  "non_goals": [
    "Provide a hosted cloud service.",
    "Provide a multi-user dashboard or authentication system.",
    "Replace git review or guarantee AI output correctness.",
    "Become a general-purpose workflow engine.",
    "Expose raw commands, environment dumps, secret values, sensitive absolute paths, private key material, token-like strings, omission placeholders, raw artifact bodies, raw manifests, raw diff bodies, validation output payloads, planner handoff payloads, raw prompt exports, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, or attacker-controlled denied path payloads.",
    "Add raw manifest export, raw artifact export, raw diff export, raw prompt export, artifact content bundling, raw artifact upload, raw manifest upload, or raw diff upload.",
    "Add new dashboard pages, CLI flags, raw export endpoints, broad file import behavior, scanners, pagination, configurable dashboard limits, configurable recent-run limits, configurable active-run limits, validation execution controls, .jjrc project defaults, or unrelated user-facing behavior as part of sanitized presentation helper maintenance or UI cleanup.",
    "Allow explicitly provided plan files outside the workspace by default.",
    "Reopen completed tasks without fresh failing validation, tests, CI, confirmed disclosure, or boundary regression evidence.",
    "Expose child command stdout or stderr from the release validation entry point or CI wrapper.",
    "Change jj status, jj runs, manifest schema, artifact routes, redaction policy, path-boundary policy, validation behavior, routes, data semantics, labels, links, fallback states, or terminal output semantics as part of sanitized presentation helper maintenance or UI cleanup."
  ],
  "open_questions": [],
  "requirements": [
    "jj run must read a non-empty Markdown plan from the explicitly provided path using documented path resolution behavior, then require the resolved plan file to remain inside the configured workspace boundary after canonicalization and symlink evaluation.",
    "Rejected external, traversal, absolute-escape, encoded-escape, symlink-escape, unreadable, malformed, or secret-looking plan inputs must produce deterministic sanitized errors before content read or artifact persistence.",
    "Accepted plan content, planner output, Codex output, command summaries, evaluation output, validation output, docs writes, manifest serialization, event writes, run artifact writes, git diff artifacts, dashboard DTOs, CLI status DTOs, CLI recent-run DTOs, and rendered dashboard or terminal data must pass through shared redaction before persistence or display.",
    "Manifest and general JSON persistence must use safe workspace, run, relative, or generic path labels and must not store raw workspace, plan, external config, git, repository, validation command, or baseline paths.",
    "Command execution records must persist sanitized argv, cwd labels, status, duration, and result metadata only; raw command text and raw environment values must not be stored or rendered.",
    "Validation stdout and stderr artifacts and git diff artifacts must be sanitized before persistence; evidence reports may include only booleans, counts, sanitized category labels, and safe relative artifact labels.",
    "Dry-run and non-dry-run modes must apply identical plan path validation, redaction, path-boundary, artifact persistence, git diff capture, diff redaction, manifest metadata, diagnostics, validation output sanitization, planner handoff sanitization, and denial behavior.",
    "jj serve must only serve files from guarded workspace and run artifact roots and must reject traversal, absolute, encoded, secret-looking, token-like, duplicate hostile query, or symlink escape inputs with deterministic denied or unavailable states.",
    "Run IDs may contain only safe letters, numbers, dots, underscores, and dashes, must not use traversal-like forms, and must be rejected when they match configured secrets or common token patterns.",
    "Run detail, history, comparison, audit export, dashboard TASK summary, dashboard latest-run, next-action, Project Docs, Recent Runs, Evaluation Findings, Active Run, Validation Status, Validation Evidence, Compare Previous, Run Artifacts, jj status, and jj runs must obtain data through shared sanitized DTO construction or shared helpers, not local raw manifest, artifact, command, environment, diff, validation output, EVAL.md body, document body, or filesystem reads after route or CLI input parsing.",
    "Untrusted, stale, partial, malformed, manually edited, or internally inconsistent metadata must render only deterministic denied, unknown, unavailable, all-clear, none, or sanitized states.",
    "The dashboard root must preserve existing sanitized TASK.md summary, latest-run summary, next-action summary, fixed Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run, and Validation Status behavior.",
    "Existing guarded run detail views must preserve compact Validation Evidence, Compare Previous, and Run Artifacts behavior using only shared sanitized DTO/helper output and guarded URL decisions.",
    "jj status and jj runs must exit successfully in a valid workspace and print compact sanitized summaries using only shared sanitized DTO/helper output and deterministic none, unavailable, unknown, or denied states.",
    "Sanitized presentation helpers may consolidate common safe run ID, status, evaluation, validation, timestamp, detail URL, audit URL, CLI run-summary line, ordered CLI run-summary fields, numbered label selection, guarded dashboard links, dashboard root section assembly, dashboard summary-row construction, TASK summary construction, next-action construction, run-detail header construction, run-detail action-link construction, and run-detail section construction only when they accept existing sanitized DTO/helper output, guarded URL decisions, fixed expected categories, route or file state metadata where applicable, or already-safe labels and preserve existing behavior.",
    "CLI run-summary presentation simplification may consolidate duplicated jj status and jj runs field ordering, numbered labels, line construction, timestamps, status, evaluation, validation text, and fallback messages only through narrow unexported CLI helpers that consume existing sanitized DTO/helper output and preserve output semantics.",
    "Guarded presentation refactors must preserve labels, ordering, visibility, guarded links, denied states, unavailable states, unknown states, none states, empty states, hostile-label handling, token-like handling, malformed or partial metadata handling, spacing, fallback messages, deterministic display data, and surrounding semantics for every touched path.",
    "Guarded presentation refactors must not introduce raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, broad visual redesign, redaction policy change, path-boundary policy change, validation behavior change, or user-facing semantic change.",
    "UI cleanup may adjust jj serve CSS, classes, and minimal template structure for clearer hierarchy, spacing, typography, action-link affordance, status and fallback styling, and responsive scanability, but must consume only existing sanitized DTO/helper output, Go template escaping, and guarded URL decisions.",
    "UI cleanup must preserve labels, ordering, visibility, guarded links, denied states, unavailable states, unknown states, none states, empty states, hostile-label handling, token-like handling, malformed or partial metadata handling, spacing-sensitive semantics, fallback messages, deterministic display data, and surrounding behavior for every touched path.",
    "UI cleanup must not introduce raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, redaction policy change, path-boundary policy change, validation behavior change, or new data semantics.",
    "Run-detail UI cleanup may adjust CSS, classes, and minimal template structure for the existing run header, run ID/status/timestamp presentation, top action links, Validation Evidence, Compare Previous, and Run Artifacts sections only, while preserving all sanitized data semantics and guarded decisions.",
    "Auto or balanced task proposal resolution must choose bugfix only from positive current blocker evidence and security only from concrete release-gate, CI, validation, test, disclosure, or boundary evidence showing a security or privacy regression.",
    "scripts/validate.sh must remain the single documented release validation entry point, run focused security boundary suites, full tests, vet, CLI build, and diff-check, suppress child command output, emit only deterministic sanitized metadata, and exit non-zero on any failing step.",
    "Completed security guardrails must remain closed unless scripts/validate.sh, focused Go security suites, full tests, vet, build, diff-check, or the CI release gate produces a concrete disclosure, path-boundary, artifact, dashboard, planner handoff, validation output, git diff, command metadata, or release-gate output regression."
  ],
  "summary": "jj is a CLI that turns a planning file into implementation-ready specs, tasks, Codex runs, validation artifacts, and a local dashboard. Security remains the controlling constraint: generated artifacts, served run-inspection surfaces, explicit plan inputs, command metadata, manifests, validation outputs, git diff artifacts, planner and Codex handoff metadata, run metadata, dashboard summaries, project document shortcuts, recent runs, evaluation findings, active-run progress, validation status, run-detail validation evidence, compare-to-previous links, sanitized run artifact inventory, CLI status output, and CLI recent-run output must be guarded, redacted, and kept inside the configured workspace or allowed run artifact roots. Completed security guardrails remain closed unless concrete validation, test, CI, disclosure, or boundary evidence proves a regression. Sanitized presentation code may continue to be simplified through narrow behavior-preserving helpers and presentation-only dashboard UI cleanup. TASK-0090 through TASK-0106 completed narrow sanitized presentation refactors and UI polish for dashboard, run-detail, and CLI run-summary surfaces while preserving behavior, routes, schemas, redaction, path-boundary behavior, validation behavior, and user-facing data semantics. TASK-0106 polished the jj serve run-detail page visual presentation for the run header, run ID/status/timestamp metadata, top action links, Validation Evidence, Compare Previous, and Run Artifacts through CSS, classes, and focused template structure using existing sanitized DTO/helper output, Go template escaping, fixed labels, safe state metadata, safe timestamps or counts, and guarded URL decisions.",
  "title": "jj Security Boundary and Dashboard Run Inspection",
  "updated_at": "2026-04-29T13:47:58Z",
  "version": 1
}
