{
  "acceptance_criteria": [
    "No persisted artifact, served dashboard response, jj status output, or jj runs output contains raw API keys, common token patterns, private key blocks, configured secret values, raw command text, raw environment values, unsafe absolute paths, omission placeholders, raw artifact bodies, raw manifests, raw diff bodies, validation output payloads, planner handoff payloads, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, or attacker-controlled denied path payloads.",
    "Traversal attempts using relative, absolute, encoded, or symlinked paths cannot read or write outside the configured workspace or allowed run artifact roots.",
    "Dry-run and non-dry-run produce equivalent redaction, boundary, diagnostic, validation output, planner handoff metadata, git diff redaction behavior, artifact persistence, and safe path label persistence.",
    "Existing TASK.md summary, latest-run summary, next-action summary, Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run, Validation Status dashboard behavior, run detail Validation Evidence, Compare Previous, Run Artifacts behavior, jj status behavior, and jj runs behavior remain unchanged when sanitized presentation helper maintenance is performed.",
    "jj status and jj runs exit successfully in a valid workspace, use only shared sanitized DTO/helper output, and render deterministic none, unavailable, unknown, or denied states for malformed, denied, stale, hostile-label, token-like, partial, tied timestamp, missing timestamp, or internally inconsistent metadata without panics or unsafe reflection.",
    "Focused CLI and serve tests cover the sanitized status, recent-runs, dashboard root, TASK summary, run detail, validation evidence, compare-previous, run artifact states, Project Docs, Evaluation Findings, Latest Run action-link, Active Run, and Validation Status dashboard behavior states touched by each task.",
    "Completed security guardrails remain closed after TASK-0065 through TASK-0072 because focused serve and CLI tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0073 centralized dashboard-root Validation Status item construction, state messages, safe count labels, timestamp labels, deterministic display data, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0073 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, safe count labels, timestamp labels, validation states, deterministic display data, or already-safe labels while preserving dashboard-root Validation Status labels, ordering, visibility, guarded links, no-validation states, unavailable states, unknown states, denied states, malformed metadata handling, partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "TASK-0073 introduced no raw manifest, artifact body, raw path, command text, environment value, diff, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new dashboard page, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing output semantic change.",
    "TASK-0073 added or preserved focused serve coverage for Validation Status normal, no-validation, unavailable, unknown, denied, malformed or partial metadata, stale metadata, hostile-label, token-like, missing timestamp, and tied timestamp states where practical.",
    "TASK-0073 validation passed focused dashboard Validation Status serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0073 because focused dashboard Validation Status serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0074 centralized dashboard-root Project Docs shortcut construction, route-error state mapping, file-state mapping, availability labels, safe label handling, and guarded URL decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0074 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, route or file state metadata, fixed safe labels, or already-safe labels while preserving Project Docs shortcut labels, ordering, visibility, guarded links, unavailable states, denied states, none states, missing metadata behavior, hostile-label handling, token-like handling, malformed or partial metadata handling, and surrounding dashboard-root behavior.",
    "TASK-0074 introduced no raw document body, raw path, raw manifest, artifact body, raw command text, raw environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new dashboard page, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing output semantic change.",
    "TASK-0074 added or preserved focused serve coverage for Project Docs normal, unavailable, denied, missing or none, malformed or hostile-label, and token-like states where practical.",
    "TASK-0074 validation passed go test ./internal/serve -run 'TestDashboardProjectDocsShortcuts', go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0074 because focused Project Docs/dashboard serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0075 centralized dashboard-root Evaluation Findings base guarded links, unavailable and denied states, state and message decisions, item construction, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0075 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, or already-safe labels while preserving Evaluation Findings labels, ordering, visibility, guarded links, all-clear states, empty states, unavailable states, unknown states, denied states, hostile-label handling, token-like handling, malformed metadata handling, stale metadata handling, partial metadata handling, and surrounding dashboard-root behavior.",
    "TASK-0075 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new dashboard page, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing output semantic change.",
    "TASK-0075 added or preserved focused serve coverage for Evaluation Findings normal, all-clear, empty, unavailable, denied, unknown, malformed or partial metadata, stale metadata, hostile-label, token-like, and guarded-link states where practical.",
    "TASK-0075 validation passed focused Evaluation Findings serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0075 because focused Evaluation Findings serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0076 centralized dashboard-root Next Action static state selection, fixed labels and messages, TASK-unavailable handling, empty or none states, unavailable states, unknown states, denied states, and guarded link construction in narrow helper logic or an equivalent simplified structure.",
    "TASK-0076 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, or already-safe labels while preserving Next Action labels, ordering, visibility, fallback messages, guarded links, empty states, denied states, unavailable states, unknown states, TASK-unavailable behavior, static summary behavior, and surrounding dashboard-root behavior.",
    "TASK-0076 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0076 added or preserved focused serve coverage for Next Action normal, empty or none, TASK-unavailable, denied, unavailable, unknown, malformed or partial, hostile-label, token-like, and guarded-link states where practical.",
    "TASK-0076 validation passed go test ./internal/serve -run 'TestDashboardNextAction', go test ./internal/serve -run 'TestSecurityReleaseGateInspectionRoutesUseSharedGuardedHelpers', go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0076 because focused dashboard Next Action serve tests, the security release-gate helper inspection test, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0077 centralized dashboard-root TASK summary construction, message fallback, next-task sanitization, empty or none handling, unavailable states, unknown states, denied states, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0077 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, or already-safe labels while preserving TASK summary labels, ordering, visibility, guarded links, fallback messages, empty states, denied states, unavailable states, unknown states, malformed or partial metadata handling, hostile-label handling, token-like handling, and surrounding dashboard-root behavior.",
    "TASK-0077 introduced no raw TASK.md body, document body, manifest body, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0077 added or preserved focused serve coverage for TASK summary normal, empty or none, unavailable, unknown, denied, malformed or partial, internally inconsistent, hostile-label, token-like, and guarded-link states where practical.",
    "TASK-0077 validation passed focused TASK summary serve tests, go test ./internal/serve -run 'TestDashboard(Task|NextAction)|TestSecurityReleaseGateInspectionRoutesUseSharedGuardedHelpers', go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0077 because focused TASK summary serve tests, the security release-gate helper inspection test, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0078 centralized common sanitized CLI run-summary field construction for jj status and jj runs in narrow option-driven helper logic while preserving output semantics.",
    "TASK-0078 consumes only existing sanitized DTO/helper output, safe counts, timestamp labels, guarded labels, fixed safe labels, or already-safe labels while preserving jj status TASK summary, latest run summary, next-action, Active Run, Validation Status, jj runs recent-run labels, ordering, spacing, provider/result/evaluation/validation/timestamp fields, fallback states, and deterministic ordering.",
    "TASK-0078 introduced no raw artifact body, manifest body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new CLI flag, route, schema field, config behavior, pagination, scanner, upload, dashboard behavior, raw export, or broad file browsing.",
    "TASK-0078 added or preserved focused CLI coverage for normal, none, unavailable, unknown, denied, malformed or partial metadata, hostile-label, token-like, missing timestamp, tied timestamp, Active Run, Validation Status, and recent-run states where practical.",
    "TASK-0078 validation passed go test ./internal/cli -run 'Test(StatusCommand|RunsCommand|CLIRunSummaryLine)', go test ./internal/cli, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0078 because focused jj status and jj runs CLI tests, go test ./internal/cli, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0079 centralized dashboard-root Latest Run action-link construction, state-specific action ordering, safe labels, unavailable, denied, unknown, none decisions, and guarded URL decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0079 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, or already-safe labels while preserving Latest Run action labels, ordering, visibility, guarded URLs, none states, denied states, unavailable states, unknown states, malformed or partial metadata handling, stale metadata behavior, hostile-label handling, token-like handling, unsafe-link behavior, and surrounding dashboard-root behavior.",
    "TASK-0079 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0079 added or preserved focused serve coverage for Latest Run action-link normal, no latest run, denied, unavailable, unknown, malformed or partial metadata, hostile-label, token-like, guarded-link, and ordering states where practical.",
    "TASK-0079 validation passed focused Latest Run dashboard tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0079 because focused Latest Run dashboard tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0080 centralized dashboard-root Recent Runs item construction, state messages, timestamp labels, deterministic display data, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0080 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, safe count labels, timestamp labels, or already-safe labels while preserving Recent Runs labels, ordering, spacing, visibility, provider/result/evaluation/validation fields, timestamp behavior, guarded URLs, empty states, unavailable states, unknown states, denied states, malformed or partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "TASK-0080 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0080 added or preserved focused serve coverage for Recent Runs normal, empty or none, unavailable, denied, unknown, malformed or partial metadata, stale metadata, hostile-label, token-like, missing timestamp, tied timestamp, and guarded-link states where practical.",
    "TASK-0080 validation passed go test ./internal/serve -run 'TestDashboardRecentRuns', go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0080 because focused Recent Runs dashboard tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0081 centralized run-detail Run Artifacts fixed item construction, safe availability labels, state messages, unavailable, none, denied, and unknown decisions, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0081 consumes only existing sanitized DTO/helper output, fixed expected artifact categories, guarded URL decisions, safe availability labels, or already-safe labels while preserving run-detail Run Artifacts labels, ordering, visibility, guarded links, state messages, none states, unavailable states, denied states, unknown states, malformed or partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, and surrounding run-detail behavior.",
    "TASK-0081 introduced no raw artifact body, raw manifest, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0081 added or preserved focused serve coverage for Run Artifacts normal, none or missing metadata, unavailable, denied, unknown, malformed or partial metadata, stale metadata, hostile-label, token-like, and guarded-link states where practical, including fixed category and link coverage, unknown-state coverage, helper-use structure checks, and no-raw-read structure checks.",
    "TASK-0081 validation passed go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0081 because Run Artifacts serve coverage, security release-gate helper structure checks, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0082 centralized run-detail Validation Evidence visible and none state decisions in validationEvidenceVisibleInputForRun, with validationEvidenceVisibleSummary owning count-label construction and safe-label copying, in narrow helper logic while preserving DTO and template output semantics.",
    "TASK-0082 consumes only existing sanitized DTO/helper output, guarded URL decisions, safe count labels, timestamp labels, validation states, sanitized validation step or category labels, fixed safe labels, or already-safe labels while preserving run-detail Validation Evidence labels, ordering, visibility, count labels, timestamp behavior, guarded links, normal states, none states, unavailable states, unknown states, denied states, malformed or partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, and surrounding run-detail behavior.",
    "TASK-0082 introduced no raw validation payload, raw manifest, artifact body, raw path, command text, environment value, diff body, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0082 added or preserved focused serve coverage for Validation Evidence normal, none, unavailable, denied, unknown, malformed or partial metadata, stale metadata, hostile-label, token-like, missing timestamp, count-label, timestamp, guarded-link, and tampered metadata sanitization states where practical.",
    "TASK-0082 validation passed focused Validation Evidence and security release-gate serve tests, tampered run metadata DTO sanitization tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0082 because focused Validation Evidence and security serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0083 centralized run-detail Compare Previous visible-summary labels, none, unavailable, denied, and unknown messages, deterministic previous-run display data, and guarded comparison URL decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0083 consumes only existing sanitized DTO/helper output, guarded URL decisions, fixed safe labels, timestamp labels, or already-safe labels while preserving run-detail Compare Previous labels, ordering, visibility, guarded URLs, deterministic previous-run selection, no-previous behavior, unavailable, unknown, and denied states, malformed or partial metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, unsafe-link behavior, and surrounding run-detail behavior.",
    "TASK-0083 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0083 added or preserved focused serve coverage for Compare Previous normal, no previous run, denied, unavailable, unknown, malformed or partial metadata, hostile-label, token-like, missing timestamp, tied timestamp, and unsafe guarded-link states where practical.",
    "TASK-0083 validation passed focused Compare Previous serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0083 because focused Compare Previous serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0084 centralized dashboard-root Active Run item construction, state messages, safe labels, timestamp labels, deterministic display data, and guarded link decisions in narrow helper logic or an equivalent simplified structure.",
    "TASK-0084 consumes only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, timestamp labels, deterministic display data, or already-safe labels while preserving dashboard-root Active Run labels, ordering, visibility, guarded links, no-active states, unavailable states, unknown states, denied states, malformed or partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "TASK-0084 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0084 added or preserved focused serve coverage for Active Run normal, no-active, unavailable, denied, unknown, malformed or partial metadata, stale metadata, hostile-label, token-like, missing timestamp, tied timestamp, and guarded-link states where practical, including helper-use structure checks and raw-read guardrails.",
    "TASK-0084 validation passed focused Active Run serve and security release-gate tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0084 because focused Active Run serve tests, security release-gate helper structure checks, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0085 simplified serve recent-run DTO safe presentation fallback branching by centralizing repeated unknown fallback normalization into narrow unexported helpers while preserving dashboard Recent Runs output semantics.",
    "TASK-0085 consumes only existing sanitized RecentRunItem DTO data and already-safe labels while preserving recent-run state, status, provider/result, evaluation, validation, timestamp fallback behavior, guarded links, ordering, spacing, deterministic display behavior, and surrounding serve semantics.",
    "TASK-0085 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, new route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0085 added focused serve coverage for recent-run safe presentation fallback normalization states where practical.",
    "TASK-0085 validation passed go test ./internal/serve -run TestRecentRunItemFromDashboardNormalizesSafePresentationStates, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0085 because focused recent-run serve tests, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression.",
    "TASK-0086 centralized invalid-run presentation state mapping for dashboard Validation Status unavailable state, run-detail Validation Evidence unavailable state, and dashboard Evaluation Findings unavailable state in narrow unexported helper logic.",
    "TASK-0086 consumes sanitized runLink DTO data and returns only fixed denied or unavailable labels while preserving touched dashboard root and run-detail labels, ordering, visibility, guarded links, spacing, fallback messages, deterministic display data, malformed or partial metadata handling, hostile-label handling, token-like handling, and surrounding behavior.",
    "TASK-0086 introduced no raw manifest, artifact body, raw path, command text, environment value, diff body, validation payload, planner handoff payload, TASK.md body, EVAL.md body, document body, configured secret, token-like value, omission placeholder, arbitrary file read, unsafe path read, route, CLI flag, schema field, config behavior, pagination, scanner, upload, broad file browsing, raw export, visual redesign, or user-facing semantic change.",
    "TASK-0086 added focused serve coverage for sanitized invalid-run presentation state fixed denied and unavailable labels, hostile labels, token-like metadata, and preserved Validation Status, Validation Evidence, and Evaluation Findings states through focused serve tests.",
    "TASK-0086 validation passed go test ./internal/serve -run 'TestSanitizedInvalidRunPresentationStateUsesFixedLabels|TestDashboardValidationStatusUnavailableUnknownDeniedAndNoneStatesAreSafe|TestRunDetailValidationEvidenceStatesAreSafe|TestDashboardEvaluationFindingsUnavailableDeniedUnknownAndNeedsWorkStates', go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh.",
    "Completed security guardrails remain closed after TASK-0086 because focused sanitized invalid-run presentation state serve tests, Validation Status, Validation Evidence, and Evaluation Findings serve tests, go test ./internal/serve, go test ./internal/cli ./internal/serve, go test ./..., git diff --check, and scripts/validate.sh passed and no current release-gate, CI, validation, test, disclosure, or boundary evidence shows a concrete regression."
  ],
  "created_at": "2026-04-28",
  "goals": [
    "Generate concrete specs and tasks from a single plan file while preserving an auditable run history.",
    "Keep generated artifacts, served files, plan inputs, workspace mutations, validation outputs, git diff artifacts, planner and Codex handoff metadata, dashboard summaries, active-run progress, validation status, run detail validation evidence, compare-to-previous links, run artifact inventory, CLI status output, CLI recent-run output, and run inspection surfaces inside the resolved workspace or guarded run artifact roots.",
    "Prevent secrets, token-like values, private key material, omission placeholders, raw commands, raw environment values, unsafe paths, raw manifests, raw artifact bodies, raw diff bodies, validation output payloads, planner handoff payloads, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, and attacker-controlled denied payloads from being persisted or rendered.",
    "Provide a dashboard-first local jj serve experience with guarded run detail, history, comparison, sanitized audit export views, compact sanitized TASK.md work-queue summary, latest-run summary, next-action summary, Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run progress, Validation Status on the dashboard root, compact validation evidence on existing run detail views, guarded compare-to-previous navigation from run detail views, and compact sanitized run artifact inventory on run detail views.",
    "Provide compact sanitized CLI summaries for quick workspace pre-flight and run-history checks without starting jj serve.",
    "Preserve dry-run and non-dry-run workflows with identical redaction, path-boundary, diagnostics, artifact persistence, diff redaction, validation, and planner handoff metadata behavior.",
    "Ensure auto and balanced task proposal resolution selects bugfix or security only from concrete current evidence, not completed guardrails, healthy context, or absent evidence.",
    "Use scripts/validate.sh as the single documented release validation entry point for local and CI security release gates.",
    "Keep completed security guardrails closed unless a concrete release-gate, CI, validation, test, disclosure, or boundary regression is observed.",
    "Keep sanitized CLI, dashboard root, run-detail, TASK summary, next-action, Project Docs, Recent Runs, Evaluation Findings, Active Run, Validation Status, Latest Run action-link, Validation Evidence, Compare Previous, Run Artifacts, and run-summary presentation code maintainable by reducing unnecessary duplication, nesting, and branching without changing output semantics."
  ],
  "non_goals": [
    "Provide a hosted cloud service.",
    "Provide a multi-user dashboard or authentication system.",
    "Replace git review or guarantee AI output correctness.",
    "Become a general-purpose workflow engine.",
    "Expose raw commands, environment dumps, secret values, sensitive absolute paths, private key material, token-like strings, omission placeholders, raw artifact bodies, raw manifests, raw diff bodies, validation output payloads, planner handoff payloads, raw prompt exports, raw TASK.md bodies, raw EVAL.md bodies, raw document bodies, or attacker-controlled denied path payloads.",
    "Add raw manifest export, raw artifact export, raw diff export, raw prompt export, artifact content bundling, raw artifact upload, raw manifest upload, or raw diff upload.",
    "Add new dashboard pages, CLI flags beyond jj status and jj runs, raw export endpoints, broad file import behavior, scanners, pagination, configurable dashboard limits, configurable recent-run limits, configurable active-run limits, validation execution controls, .jjrc project defaults, or unrelated user-facing behavior as part of dashboard summary, run detail validation evidence, compare-to-previous work, run artifact inventory work, CLI status work, recent-runs CLI work, Project Docs shortcut work, Evaluation Findings work, Active Run work, Validation Status work, Latest Run action-link work, Recent Runs presentation work, or sanitized presentation helper maintenance.",
    "Allow explicitly provided plan files outside the workspace by default.",
    "Reopen completed tasks without fresh failing validation, tests, CI, confirmed disclosure, or boundary regression evidence.",
    "Expose child command stdout or stderr from the release validation entry point or CI wrapper.",
    "Change jj status, jj runs, dashboard layout, manifest schema, artifact routes, redaction policy, path-boundary policy, validation behavior, or user-facing output semantics as part of sanitized presentation helper maintenance."
  ],
  "open_questions": [],
  "requirements": [
    "jj run must read a non-empty Markdown plan from the explicitly provided path using documented path resolution behavior, then require the resolved plan file to remain inside the configured workspace boundary after canonicalization and symlink evaluation.",
    "Rejected external, traversal, absolute-escape, encoded-escape, symlink-escape, unreadable, malformed, or secret-looking plan inputs must produce deterministic sanitized errors before content read or artifact persistence.",
    "Accepted plan content, planner output, Codex output, command summaries, evaluation output, validation output, docs writes, manifest serialization, event writes, run artifact writes, git diff artifacts, dashboard DTOs, CLI status DTOs, CLI recent-run DTOs, and rendered dashboard or terminal data must pass through shared redaction before persistence or display.",
    "Manifest and general JSON persistence must use safe workspace, run, relative, or generic path labels and must not store raw workspace, plan, external config, git, repository, validation command, or baseline paths.",
    "Command execution records must persist sanitized argv, cwd labels, status, duration, and result metadata only; raw command text and raw environment values must not be stored or rendered.",
    "Validation stdout and stderr artifacts and git diff artifacts must be sanitized before persistence; evidence reports may include only booleans, counts, sanitized category labels, and safe relative artifact labels.",
    "Dry-run and non-dry-run modes must apply identical plan path validation, redaction, path-boundary, artifact persistence, git diff capture, diff redaction, manifest metadata, diagnostics, validation output sanitization, planner handoff sanitization, and denial behavior.",
    "jj serve must only serve files from guarded workspace and run artifact roots and must reject traversal, absolute, encoded, secret-looking, token-like, duplicate hostile query, or symlink escape inputs with deterministic denied or unavailable states.",
    "Run IDs may contain only safe letters, numbers, dots, underscores, and dashes, must not use traversal-like forms, and must be rejected when they match configured secrets or common token patterns.",
    "Run detail, history, comparison, audit export, dashboard TASK summary, dashboard latest-run, next-action, Project Docs, Recent Runs, Evaluation Findings, Active Run, Validation Status, Validation Evidence, Compare Previous, Run Artifacts, jj status, and jj runs must obtain data through shared sanitized DTO construction or shared helpers, not local raw manifest, artifact, command, environment, diff, validation output, EVAL.md body, document body, or filesystem reads after route or CLI input parsing.",
    "Untrusted, stale, partial, malformed, manually edited, or internally inconsistent manifest fields, artifact references, validation summaries, command metadata, diff metadata, provider fields, timestamps, evaluation metadata, findings, active-run metadata, validation-status metadata, validation-evidence metadata, compare-previous metadata, run-artifact metadata, status metadata, recent-run metadata, TASK-summary metadata, and next-action hints must render only deterministic denied, unknown, unavailable, all-clear, none, or sanitized states.",
    "The dashboard root must preserve existing sanitized TASK.md summary, latest-run summary, next-action summary, fixed Project Docs shortcuts, Recent Runs, Evaluation Findings, Active Run, and Validation Status behavior.",
    "Dashboard-root TASK summary presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, or already-safe labels while preserving TASK summary labels, ordering, guarded links, fallback messages, empty states, denied states, unavailable states, unknown states, malformed or partial metadata handling, hostile-label handling, token-like handling, and surrounding dashboard-root behavior.",
    "Dashboard-root next-action presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, or already-safe labels while preserving next-action labels, ordering, guarded links, empty states, denied states, unavailable states, unknown states, and surrounding dashboard-root behavior.",
    "Dashboard-root Project Docs shortcut presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, route or file state metadata, and fixed already-safe labels while preserving shortcut labels, ordering, guarded links, unavailable states, denied states, none states, and surrounding dashboard-root behavior.",
    "Dashboard-root Project Docs shortcut presentation logic should centralize per-shortcut construction, route-error state mapping, file-state mapping, label sanitization, availability labels, and guarded URL decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root Evaluation Findings presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, and already-safe labels while preserving labels, ordering, guarded links, all-clear states, empty states, unavailable states, unknown states, denied states, hostile-label handling, token-like label handling, malformed metadata handling, stale metadata handling, partial metadata handling, and surrounding dashboard-root behavior.",
    "Dashboard-root Latest Run action links must be constructed through shared guarded helpers that allow only safe Run detail, Run history, and Audit export labels and guarded URLs while preserving state-specific action ordering and visibility.",
    "Dashboard-root Latest Run action-link presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, or fixed already-safe labels while preserving labels, ordering, visibility, guarded URLs, none states, denied states, unavailable states, unknown states, malformed or partial metadata handling, stale metadata behavior, hostile-label handling, token-like handling, unsafe-link behavior, and surrounding dashboard-root behavior.",
    "Dashboard-root Latest Run action-link presentation logic should centralize state-specific action ordering, safe labels, none, unavailable, denied, unknown decisions, and guarded URL decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root Recent Runs presentation may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, safe count labels, timestamp labels, or already-safe labels while preserving labels, ordering, spacing, visibility, provider/result/evaluation/validation fields, timestamp behavior, guarded URLs, empty states, unavailable states, unknown states, denied states, malformed or partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "Dashboard-root Recent Runs presentation logic should centralize item construction, state messages, timestamp labels, deterministic display data, and guarded link decisions in narrow helper logic or an equivalent simplified structure without changing rendered behavior.",
    "Recent-run DTO fallback presentation logic may be simplified through narrow unexported helpers that consume existing sanitized recent-run item data and preserve unknown fallback behavior for state, status, provider or result, evaluation state, validation state, and timestamp label fields without changing user-facing dashboard semantics.",
    "The dashboard root must show a compact Active Run section when at least one valid guarded non-terminal run exists, using only shared sanitized run DTO or helper output.",
    "Each Active Run item must expose only sanitized run ID label, status, provider or result label, evaluation state when available, timestamp label, and guarded links to existing run views.",
    "Active Run selection must be deterministic when timestamps are missing, malformed, duplicated, or tied, using sanitized stable tie-breakers only.",
    "No-active, missing metadata, denied, malformed, partial, stale, hostile-label, token-like, or internally inconsistent active-run metadata must render deterministic none, unavailable, unknown, or denied states without server errors or unsafe reflection.",
    "Dashboard-root Active Run presentation logic may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, or already-safe labels while preserving item labels, ordering, visibility, guarded links, state messages, timestamp labels, deterministic display data, no-active states, unavailable states, unknown states, denied states, malformed metadata handling, partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "Dashboard-root Active Run presentation logic should centralize item construction, state messages, safe labels, timestamp labels, deterministic display data, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "The dashboard root must show a compact Validation Status section when at least one valid guarded completed run has validation metadata, using only shared sanitized run DTO or helper output.",
    "Each Validation Status item must expose only sanitized run ID label, validation state, safe count labels when available, timestamp label, and guarded links to existing run views.",
    "Validation Status selection must be deterministic when timestamps are missing, malformed, duplicated, or tied, using sanitized stable tie-breakers only.",
    "No-validation, missing metadata, denied, malformed, partial, stale, hostile-label, token-like, or internally inconsistent validation metadata must render deterministic none, unavailable, unknown, or denied states without server errors or unsafe reflection.",
    "Dashboard-root Validation Status presentation logic may be simplified through narrow helpers that consume only existing sanitized dashboard DTO/helper output, guarded URL decisions, safe count labels, timestamp labels, validation states, deterministic display data, or already-safe labels while preserving labels, ordering, visibility, guarded links, no-validation states, unavailable states, unknown states, denied states, malformed metadata handling, partial metadata handling, stale metadata handling, hostile-label handling, token-like handling, missing timestamp behavior, tied timestamp behavior, and surrounding dashboard-root behavior.",
    "Dashboard-root Validation Status presentation logic should centralize item construction, state messages, safe count labels, timestamp labels, deterministic display data, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Existing guarded run detail views must show a compact Validation Evidence section when the selected guarded completed run has validation metadata.",
    "The Validation Evidence section must expose only sanitized run ID label, validation state, safe pass/fail/skip/error count labels when available, timestamp label, sanitized validation step or category labels, and guarded links to existing safe views.",
    "Missing, denied, malformed, partial, stale, hostile-label, token-like, or internally inconsistent validation evidence metadata must render deterministic none, unavailable, unknown, or denied states without server errors or unsafe reflection.",
    "Run-detail Validation Evidence presentation logic may be simplified through narrow helpers that consume only existing sanitized DTO/helper output, guarded URL decisions, or already-safe labels while preserving labels, count labels, state messages, guarded links, ordering, visibility, and none, unavailable, unknown, or denied behavior.",
    "Run-detail Validation Evidence presentation logic should centralize visible-summary labels, count labels, state messages, guarded links, and none, unavailable, unknown, or denied decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Existing guarded run detail views must show a compact Compare Previous action when a deterministic previous valid guarded run exists.",
    "The Compare Previous action must expose only sanitized current run label, sanitized previous run label, and a guarded link to the existing comparison view.",
    "Runs with no previous comparable run must render a deterministic none or unavailable state without server errors.",
    "Previous-run selection must be deterministic when timestamps are missing, malformed, duplicated, or tied, using sanitized stable tie-breakers only.",
    "Run-detail Compare Previous presentation logic may be simplified through narrow helpers that consume only existing sanitized DTO/helper output, guarded URL decisions, fixed safe labels, timestamp labels, or already-safe labels while preserving labels, ordering, visibility, guarded URLs, deterministic previous-run behavior, and none, unavailable, unknown, denied, malformed, partial, hostile-label, token-like, missing timestamp, tied timestamp, and unsafe URL behavior touched by the refactor.",
    "Run-detail Compare Previous presentation logic should centralize visible-summary labels, none, unavailable, denied, and unknown messages, deterministic previous-run display data, and guarded comparison URL decisions in a narrow helper or equivalent simplified structure without changing rendered behavior or deterministic previous-run selection.",
    "Existing guarded run detail views must show a compact Run Artifacts section for valid guarded runs.",
    "The Run Artifacts section must list only fixed expected artifact categories and safe availability labels, such as input plan, generated SPEC, generated TASK, evaluation, manifest summary, git diff summary, and Codex summary when present.",
    "Available Run Artifacts items must link only to existing guarded safe views or existing sanitized artifact routes.",
    "Runs with no artifact metadata must render a deterministic none or unavailable state without server errors.",
    "Denied, malformed, stale, partial, hostile-label, token-like, or internally inconsistent artifact metadata must render deterministic denied, unknown, unavailable, or none states without unsafe reflection.",
    "Run Artifacts data must be derived from shared sanitized run DTO/helper output and must not enumerate arbitrary files or add broad file browsing behavior.",
    "Missing, malformed, denied, stale, hostile-label, token-like, partial, or internally inconsistent run metadata must not render unsafe labels, raw paths, raw manifests, raw diffs, raw commands, raw environment values, validation payloads, or artifact bodies.",
    "jj status must exit successfully in a valid workspace and print a compact sanitized TASK summary, latest run summary, next action summary, Active Run when present, and Validation Status when present.",
    "jj status output must use only shared sanitized DTO/helper output and expose safe labels only: status names, safe counts, sanitized run labels, provider or result labels, evaluation or validation states, timestamp labels, and deterministic none, unavailable, unknown, or denied states.",
    "jj status must not add raw artifact browsing, raw export, new config semantics, or dashboard behavior changes.",
    "jj runs must exit successfully in a valid workspace and print a compact sanitized recent-run list.",
    "jj runs output must use only shared sanitized DTO/helper output and expose safe labels only: sanitized run labels, status or result labels, provider labels when available, evaluation states when available, validation states when available, timestamp labels, and deterministic none, unavailable, unknown, or denied states.",
    "jj runs ordering must be deterministic when timestamps are missing, malformed, duplicated, or tied, using sanitized stable tie-breakers only.",
    "jj runs must not add raw artifact browsing, raw export, new config semantics, pagination, dashboard behavior changes, scanners, artifact uploads, broad file browsing, .jjrc behavior, or unrelated user-facing features.",
    "Active Run, Validation Status, Validation Evidence, Compare Previous, Run Artifacts, jj status, and jj runs rendering must not add new dashboard pages, raw export endpoints, scanners, artifact uploads, broad file browsing, pagination, configurable limits, or unrelated user-facing features.",
    "Sanitized presentation helpers may consolidate common safe run ID, status, evaluation, validation, timestamp, detail URL, audit URL, CLI run-summary line, ordered CLI run-summary field construction, numbered label selection, dashboard-root guarded action-link construction, dashboard-root Latest Run action-link construction, dashboard-root Recent Runs item construction, Recent Runs safe presentation fallback construction, dashboard-root TASK summary construction, dashboard-root next-action construction, Project Docs shortcut construction, Validation Evidence visible-summary construction, Compare Previous visible-summary and guarded-link construction, run-detail Run Artifacts item construction, dashboard-root Evaluation Findings summary construction, dashboard-root Active Run item construction, and dashboard-root Validation Status item construction only when they accept existing sanitized DTO/helper output, guarded URL decisions, fixed expected categories, route or file state metadata where applicable, or already-safe labels and preserve existing behavior.",
    "Common sanitized CLI run-summary field construction may be centralized in narrow helpers shared by jj status and jj runs where behavior is identical, including Latest Run, Active Run, and recent-run entries, while preserving exact output labels, ordering, spacing, fallback states, timestamp handling, active-run behavior, validation-status behavior, next-action behavior, and deterministic ordering.",
    "Sanitized next-action presentation helpers may consolidate next-action labels, messages, guarded links, empty states, denied states, unavailable states, unknown states, static summaries, and TASK-unavailable states only when they consume existing sanitized dashboard DTO/helper output, guarded URL decisions, or already-safe labels and preserve surrounding dashboard-root behavior.",
    "Sanitized Project Docs shortcut presentation helpers may consolidate per-shortcut construction, route-error state mapping, file-state mapping, label sanitization, availability labels, and guarded URL decisions only when they accept existing sanitized dashboard DTO/helper output, guarded URL decisions, route or file state metadata, or fixed already-safe labels and preserve existing behavior.",
    "Sanitized Recent Runs presentation helpers may consolidate item construction, state messages, timestamp labels, deterministic display data, guarded link decisions, and safe field fallback mapping only when they accept existing sanitized dashboard DTO/helper output, guarded URL decisions, fixed safe labels, safe count labels, timestamp labels, or already-safe labels and preserve existing behavior.",
    "Refactored CLI and dashboard presentation paths must remain thin and must not introduce raw manifest, artifact, command, environment, diff, validation output, planner handoff, TASK.md, EVAL.md, document body, arbitrary file, unsafe path reads, new user-facing surfaces, output semantics, schema fields, routes, configuration, pagination, scanners, uploads, or broad file browsing.",
    "Unnecessary duplicated or deeply nested sanitized CLI, dashboard root, dashboard TASK summary, dashboard Latest Run action-link, dashboard Recent Runs, Project Docs shortcut, dashboard Active Run, dashboard Validation Status, dashboard run-detail, and validation-evidence presentation branching should be simplified only in narrow areas where existing behavior can be preserved and validated.",
    "Run-detail Run Artifacts presentation logic should centralize fixed artifact item construction, safe availability labels, state messages, unavailable, none, denied, and unknown decisions, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root Evaluation Findings presentation logic should centralize base guarded links, unavailable and denied states, state and message decisions, item construction, and guarded link decisions in narrow helpers or equivalent simplified structures without changing rendered behavior.",
    "Dashboard-root next-action presentation logic should centralize static next-action summaries, TASK-unavailable states, labels, messages, guarded links, empty or none states, denied states, unavailable states, unknown states, and state decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root TASK summary presentation logic should centralize message fallback, next-task sanitization, empty-state handling, state labels, unavailable, unknown, denied, and internally inconsistent state decisions, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root Active Run presentation logic should centralize item construction, state messages, safe labels, timestamp labels, deterministic display data, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Dashboard-root Validation Status presentation logic should centralize item construction, state messages, safe count labels, timestamp labels, deterministic display data, and guarded link decisions in a narrow helper or equivalent simplified structure without changing rendered behavior.",
    "Serve invalid-run presentation state mapping may be simplified through a narrow unexported helper that consumes sanitized run DTO data and returns only fixed denied or unavailable labels while preserving dashboard Validation Status, run-detail Validation Evidence, and dashboard Evaluation Findings behavior.",
    "Serve invalid-run presentation helpers must not reflect hostile or token-like metadata and must preserve existing denied detection, unavailable fallback behavior, guarded links, labels, ordering, visibility, spacing, fallback messages, and deterministic display data for touched paths.",
    "Auto or balanced task proposal resolution must choose bugfix only from positive current blocker evidence and security only from concrete release-gate, CI, validation, test, disclosure, or boundary evidence showing a security or privacy regression.",
    "scripts/validate.sh must remain the single documented release validation entry point, run focused security boundary suites, full tests, vet, CLI build, and diff-check, suppress child command output, emit only deterministic sanitized metadata, and exit non-zero on any failing step.",
    "Completed security guardrails must remain closed unless scripts/validate.sh, focused Go security suites, full tests, vet, build, diff-check, or the CI release gate produces a concrete disclosure, path-boundary, artifact, dashboard, planner handoff, validation output, git diff, command metadata, or release-gate output regression."
  ],
  "summary": "jj is a CLI that turns a planning file into implementation-ready specs, tasks, Codex runs, validation artifacts, and a local dashboard. Security remains the controlling constraint: generated artifacts, served run-inspection surfaces, explicit plan inputs, command metadata, manifests, validation outputs, git diff artifacts, planner and Codex handoff metadata, run metadata, dashboard summaries, project document shortcuts, recent runs, evaluation findings, active-run progress, validation status, run detail validation evidence, compare-to-previous links, sanitized run artifact inventory, CLI status output, and CLI recent-run output must be guarded, redacted, and kept inside the configured workspace or allowed run artifact roots. Completed security guardrails remain closed unless concrete validation, test, CI, disclosure, or boundary evidence proves a regression. Sanitized presentation code may continue to be simplified through narrow behavior-preserving helpers, including dashboard-root helpers completed through TASK-0077, common CLI run-summary field construction shared by jj status and jj runs completed in TASK-0078, dashboard-root Latest Run action-link presentation maintenance completed in TASK-0079, dashboard-root Recent Runs presentation maintenance completed in TASK-0080, run-detail Run Artifacts presentation maintenance completed in TASK-0081, run-detail Validation Evidence presentation maintenance completed in TASK-0082, run-detail Compare Previous presentation maintenance completed in TASK-0083, dashboard-root Active Run presentation maintenance completed in TASK-0084, serve recent-run DTO fallback presentation maintenance completed in TASK-0085, and invalid-run serve presentation state mapping completed in TASK-0086.",
  "title": "jj Security Boundary and Dashboard Run Inspection",
  "updated_at": "2026-04-29T06:08:15Z",
  "version": 1
}
